# 2주차

## 1교시

```sql
-- 1. 데이터베이스 생성
-- 2. 테이블 생성
-- 부서, 고객 / dept, emp / prof, stu
-- 3. 데이터 입력
-- 4. 데이터 조회
```

```sql
CREATE SCHEMA `testdb` DEFAULT CHARACTER SET utf8mb4;
use testdb;
drop table 부서;

-- 부서(부서번호, 부서명, 위치)
CREATE TABLE 부서(
	부서코드	char(2) NOT NULL,
	부서명	varchar(20),
    위치		varchar(10),
	PRIMARY KEY(부서코드)
);

-- 부서 데이터 입력
insert into 부서 values('AA','총무부','서울');
insert into 부서 values('BB','영업부','대전');
insert into 부서 values('CC','기획부','서울');

-- 부서 데이터 조회
select * from 부서;
```

![image.png](2%EC%A3%BC%EC%B0%A8/image.png)

```sql
-- 고객(고객아이디, 이름, 나이, 등급, 직업)
CREATE TABLE 고객(
	고객아이디	char(8),
    이름		varchar(20),
    나이		int,
    등급		varchar(10),
    직업		varchar(10),
    PRIMARY KEY(고객아이디)
);

insert into 고객 values('hoho','이순신',20,'gold','교사');
insert into 고객 values('grace','홍길동',24,'gold','학생');
insert into 고객 values('mango','삼돌이',20,'silver','학생');
insert into 고객 values('juce','갑순이',20,'gold','공무원');
insert into 고객 values('orange','강감찬',20,'silver','군인');

select * from 고객;
```

![image.png](2%EC%A3%BC%EC%B0%A8/image%201.png)

## 2교시 erdCloud : dept, emp+MySQL

```sql
-- prof(ID, 이름)
create table prof(
	ID	char(4)		NOT NULL,
    이름	varchar(20),
    PRIMARY KEY(ID)
);

insert into prof values('P001','한성식');
insert into prof values('P002','김설훈');
insert into prof values('P003','박광민');

-- stu(학번, 이름, 전공, 지도교수)
CREATE TABLE stu(
	학번		char(7) 	NOT NULL,
	이름		varchar(20),
    전공		varchar(30),
    지도교수	char(4),
    PRIMARY KEY(학번),
    FOREIGN KEY(지도교수) REFERENCES prof(ID)
);

insert into stu values ('2003001','김철수','국문학','P001');
insert into stu values ('2003002','박선하','국문학','P002');
insert into stu values ('2003003','안미희','컴퓨터공학','P001');
insert into stu values ('2003004','유창식','컴퓨터공학','P003');
insert into stu values ('2003005','임한솔','산업공학','P002');

```

![image.png](2%EC%A3%BC%EC%B0%A8/image%202.png)

---

### 데이터 모델링(Data Modeling)

요구사항 분석 → 개념적 설계 → 논리적 설계 → 물리적 설계